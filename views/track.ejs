<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard - Weight&Watch</title>

  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet" />
</head>

<body class="bg-gray-900 text-white">
  <div class="container mx-auto px-4">
    <div class="secondBox">

      <div class="text-4xl font-bold mb-8">Hello, <%= user.username %>
      </div>

      <!-- Form -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

        <div class="bg-gray-800 rounded-lg p-6">
          <form action="/track" method="post">
            <input class="bg-gray-700 rounded-lg py-2 px-4 w-full focus:outline-none text-white mb-4" type="date"
              name="date" placeholder="Select date" />
            <br />
            <input class="bg-gray-700 rounded-lg py-2 px-4 w-full focus:outline-none text-white mb-4" type="number"
              name="weight" step="0.1" placeholder="Enter weight" />
            <br />
            <button class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-lg w-full" type="submit"
              name="btn" value="firstButton">
              Add data
            </button>
          </form>
          <% if (dataAdded==='1' ) { %>
            <!-- Check for dataAdded value as string '1' -->
            <p class="text-green-500 mt-2">Data Added successfully</p>
            <% } %>
        </div>

        <!-- Form for tracking data -->
        <div class="bg-gray-800 rounded-lg p-6">
          <form action="/progress" method="post">
            <select class="bg-gray-700 rounded-lg py-2 px-4 w-full focus:outline-none text-white mb-4" name="duration"
              id="">
              <option value="7">7 Days</option>
              <option value="14">14 Days</option>
              <option value="28">28 Days</option>
              <option value="60">60 Days</option>
              <option value="90">90 Days</option>
            </select>
            <br />
            <button class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-lg w-full" type="submit"
              name="btn" value="secondButton">
              Track!
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>

</html>